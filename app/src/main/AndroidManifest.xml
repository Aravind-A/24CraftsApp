<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.iyashwant.spiderprojectprototype">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".login.Login2"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.Login"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".signup.SignupPage1"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".signup.SignupPage2"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".signup.SignupPage3"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".signup.SignupPage4"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".verification.Verification"
            android:windowSoftInputMode="adjustPan"/>
    </application>

</manifest>